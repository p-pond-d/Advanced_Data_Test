{
  "name": "loymalila-analytics",
  "version": "1.0.0",
  "description": "Unified runner for Loymalila Sales & Inventory Analytics",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "etl": "npm run etl --prefix backend",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "generate:map": "node generate_map.js",
    "start": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
